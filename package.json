{
  "name": "geo-visibility-audit-mcp",
  "version": "0.1.0",
  "description": "Standalone MCP server for auditing pasted HTML for GEO and AI-search visibility readiness.",
  "type": "module",
  "bin": {
    "geo-visibility-audit-mcp": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "lint": "node --check src/server.js && node --check src/geo-audit.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "cheerio": "^1.1.2"
  },
  "overrides": {
    "file-type": "^22.0.1"
  },
  "devDependencies": {}
}
